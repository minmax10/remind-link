[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[start]
cmd = "python init_db.py && PYTHONIOENCODING=utf-8 LC_ALL=C.UTF-8 LANG=C.UTF-8 uvicorn app.main:app --host 0.0.0.0 --port $PORT"
